# @ng-bootstrap and @ngrx have not shipped Angular 22 builds yet (their latest
# releases still peer-depend on Angular 21). They are verified working on Angular 22,
# so we install with legacy peer resolution to avoid ERESOLVE failures.
# Remove this once @ng-bootstrap and @ngrx publish Angular 22-compatible releases.
legacy-peer-deps=true
